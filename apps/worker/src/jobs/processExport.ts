// Phase 5 — wired with Reporting & Exports + GDPR portability
export async function handleProcessExport(_payload: Record<string, unknown>) {
  // TODO: collect all user/building data, package as JSON/CSV, store signed URL
}
