// Phase 5 — wired with Reporting & Exports
export async function handleGeneratePdf(_payload: Record<string, unknown>) {
  // TODO: generate PDF report, store in Supabase Storage, return signed URL
}
