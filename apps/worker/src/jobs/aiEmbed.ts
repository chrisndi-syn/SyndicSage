// Phase 4 — wired with AI Gateway + pgvector
export async function handleAiEmbed(_payload: Record<string, unknown>) {
  // TODO: generate embedding via AI Gateway → store in ai_embeddings (pgvector)
}
